#:kivy 1.0.9

<MainWidget>:
    image: image

    # Scatter:
    FloatLayout:

        Image:
            id: image
            # center_x: root.width // 2
            # center_y: root.height // 2
            pos: 0, 0
            size: root.width, root.height
            texture: root.img_texture

        # Label:
            # font_size: 70
            # center_x: root.width
            # top: root.top
            # text: "0"
